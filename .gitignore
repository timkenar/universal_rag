# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets & local config
.env

# Persisted indexes and cache (regenerated by `ingest`)
storage/

# Obsidian memory vault (learned facts — remove this line to version your memories)
memory_vault/

# Ingested source files (optional — remove if you want them tracked)
files/*
!files/.gitkeep
